name: Scrape Desikahani2

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */8 * * *'

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - run: pip install requests beautifulsoup4
      
      - run: python scraper_desikahani2.py
      
      - run: |
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          git add data/videos.json
          git diff --cached --quiet || git commit -m "chore: update desikahani2 videos [skip ci]"
          git push
